<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:background="@color/white"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".user.tìmkiemsanpham.UserTimKiemSanPhamActivity">

    <!-- Toolbar chứa thanh tìm kiếm -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/tb_tim_kiem"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:layout_marginTop="24dp"
        android:background="@android:color/transparent"
        android:elevation="5dp"
        app:layout_constraintTop_toTopOf="parent"
        app:navigationIcon="@drawable/ic_quay_lai">

        <androidx.cardview.widget.CardView
            android:id="@+id/cv_tim_kiem"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="12dp"
            app:cardBackgroundColor="@color/white"
            app:cardCornerRadius="24dp"
            app:cardElevation="6dp">

            <androidx.appcompat.widget.SearchView
                android:id="@+id/sv_tim_kiem"
                android:layout_width="match_parent"
                android:layout_height="45dp"
                android:theme="@style/Theme.AppCompat.Light"
                app:iconifiedByDefault="false"
                app:queryBackground="@android:color/transparent"
                app:queryHint="Tìm kiếm sản phẩm ..."
                app:searchIcon="@drawable/ic_tim_kiem" />

        </androidx.cardview.widget.CardView>

    </androidx.appcompat.widget.Toolbar>

    <!-- Danh sách kết quả tìm kiếm -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_tim_kiem"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="8dp"
        android:layout_marginHorizontal="8dp"
        android:clipToPadding="false"
        android:paddingBottom="16dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tb_tim_kiem" />

    <!-- Thông báo khi không có kết quả (Căn giữa màn hình) -->
    <TextView
        android:id="@+id/tv_thong_bao_tim_kiem_san_pham"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="Không tìm thấy sản phẩm phù hợp"
        android:textColor="@color/Nude_pink"
        android:textSize="16sp"
        android:textStyle="italic"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tb_tim_kiem" />

</androidx.constraintlayout.widget.ConstraintLayout>